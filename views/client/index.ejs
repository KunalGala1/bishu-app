<% 
const aboutParsedBody = JSON.parse(data.about.body);


if (data.Track && data.Track.length > 0) {
    music_track = JSON.parse(data.Track[0].body);
}

if (data.Video && data.Video.length > 0) {
    video_track = JSON.parse(data.Video[0].body);
}

const music_track = JSON.parse(data.featuredTrack.body);
const video_track = JSON.parse(data.featuredVideo.body);

const contactParsedBody = JSON.parse(data.contact.body);
const contactDescription = contactParsedBody.description;
%>


<!DOCTYPE html>
<html lang="en">

	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Home</title>
		<%- include('../partials/head') %>
	</head>

	<body class="bg-red-950">
		<%- include('../partials/nav') %>
		<%- include('../partials/NameSplash') %>
		<%- include('../sections/hero') %>
		<%- include('../sections/content_preview', {aboutParsedBody, music_track, video_track}) %>
		<section class="max-w-[1200px] mx-auto p-4">
			<%- include('../partials/ContactHeader', {description: contactDescription}) %>
			<%- include('../sections/contact_form') %>
		</section>
		<%- include('../partials/footer') %>
	</body>

</html>