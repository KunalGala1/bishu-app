<% 
const aboutParsedBody = JSON.parse(data.about.body);

let music_track, video_track, blog_post;

if (data.Track && data.Track.length > 0) {
    music_track = JSON.parse(data.Track[0].body);
}

if (data.Video && data.Video.length > 0) {
    video_track = JSON.parse(data.Video[0].body);
}

if (data.Blogpost && data.Blogpost.length > 0) {
    blog_post = JSON.parse(data.Blogpost[0].body);
	blog_post_metadata = data.Blogpost[0];
}
%>


<!DOCTYPE html>
<html lang="en">

	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Home</title>
		<%- include('../partials/head') %>
	</head>

	<body class="bg-red-950">
		<%- include('../partials/nav') %>
		<%- include('../sections/hero') %>

		<%- include('../sections/content_preview', {music_track, video_track, blog_post, blog_post_metadata}) %>

		<%- include('../partials/footer') %>
	</body>

</html>